# Promoted artifacts

Only DVC pointers and small evidence manifests belong here. Candidate outputs stay
under `.local/runs`; a LoRA enters this directory only after hash verification,
evaluation and recorded approval. Third-party base weights never enter DVC.
