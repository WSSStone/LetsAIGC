# Workflow Recipe Contract

Recipe documents are strict YAML. Unknown fields and nodes fail validation. A
recipe declares intent, stability, trusted API graph, native node allowlist, models,
input bindings with ranges, outputs, resources, and revision-mutable fields.

